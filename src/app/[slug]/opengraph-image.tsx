import { ImageResponse } from "next/og";
import { createClient } from "@/lib/supabase/server";
export const alt="LinkHub profile preview"; export const size={width:1200,height:630}; export const contentType="image/png";
export default async function OpenGraphImage({params}:{params:Promise<{slug:string}>}) { const {slug}=await params; const supabase=await createClient(); const {data:profile}=await supabase.from("linktrees").select("name,bio,seo_title,seo_description,share_image_color").eq("slug",slug).single(); const title=profile?.seo_title||profile?.name||"LinkHub"; const description=profile?.seo_description||profile?.bio||`Visit /${slug}`; return new ImageResponse(<div style={{width:"100%",height:"100%",display:"flex",flexDirection:"column",justifyContent:"space-between",padding:"72px",background:profile?.share_image_color||"#171712",color:"white",fontFamily:"Arial, sans-serif"}}><div style={{display:"flex",fontSize:24,fontWeight:800,letterSpacing:8,opacity:.65}}>LINKHUB</div><div style={{display:"flex",flexDirection:"column",gap:24}}><div style={{display:"flex",maxWidth:1000,fontSize:78,lineHeight:.95,fontWeight:800,letterSpacing:-4}}>{title}</div><div style={{display:"flex",maxWidth:900,fontSize:30,lineHeight:1.35,opacity:.75}}>{description}</div></div><div style={{display:"flex",fontSize:24,fontWeight:700,opacity:.65}}>/{slug}</div></div>,size); }